import cv2
import numpy as np
import os
from pathlib import Path

# loading handwritten equation image
IMAGE_PATH = "data/handwritten_equations/equation1.png"
OUTPUT_DIR = "data/segmented_characters"
os.makedirs(OUTPUT_DIR, exist_ok=True)

# preprocess the handwritten images
def preprocess():
    pass
# find contours, detecting the characters

# extract and normalize each character


# handle overlapping or merged characters

# save segmented characters
